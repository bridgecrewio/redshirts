/* eslint-disable camelcase */
import { GitlabApiManager } from '../gitlab/gitlab-api-manager';

export class GitlabServerApiManager extends GitlabApiManager {
    // empty shell in case changes are needed later
}
